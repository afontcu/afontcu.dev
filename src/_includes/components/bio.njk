<div class="bio">
  <img
    src="/profile-pic.jpg"
    alt="{{ site.author }}"
    class="bio-avatar"
    title="Hey, this is me! :D"
  >
  {% include "components/personal-links.njk" %}
</div>

<style>
.bio {
  display: flex;
  align-items: center;
}

.bio-avatar {
  width: 80px;
  height: 80px;
  min-width: 80px;
  border-radius: 50%;
  margin-right: var(--rhythm-half);
  margin-bottom: 0;
}

/* Footer bio - centered and stacked */
.full-bleed .bio {
  flex-direction: column;
  align-items: center;
  text-align: center;
  gap: 1rem;
  max-width: 40ch;
  margin: var(--rhythm-1-5) auto 0;
}

.full-bleed .bio-avatar {
  margin-right: 0;
}
</style>
